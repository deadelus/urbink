// Tests widget Urbink — à étoffer au fur et à mesure des epics.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder — à remplacer par de vrais tests', (tester) async {
    expect(true, isTrue);
  });
}
