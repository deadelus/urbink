rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMULATOR ONLY — permissive rules for testing
    // In production, always use firestore.rules (strict RGPD/NFR13 rules)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
